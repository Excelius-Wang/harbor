# Harbor UI unification

## Goal

Deliver the completed UI phase through the Gist action module in one PR, fix/recheck actual CodeRabbit feedback, merge after checks, and hand off deferred work. The user explicitly narrowed scope and authorized merge on 2026-09-06; full-site and native acceptance move to the next session. Handoff: `/private/tmp/harbor-ui-phase-one-handoff.md`.

## Current state

- Workspace `/Users/bytedance/Documents/Work/Code/harbor`; branch `refactor/unified-harbor-ui`, base `03709c24455a67b1694c41dd13e4eb18548b9e0a`, pre-delivery committed HEAD `4f3936e`. Latest user authorization includes one phase-one PR, actual CodeRabbit fixes and merge; no release or unrelated branch cleanup. Full-site/native acceptance is deferred. On 2026-09-06 the user enabled full access: standard Git fetch and authenticated GitHub CLI now work, the account has push permission, and origin/main still matches the base. The prior connector approval gate is historical; use normal git/gh delivery. PR draft and handoff: `/private/tmp/harbor-ui-phase-one-pr.md` and `/private/tmp/harbor-ui-phase-one-handoff.md`.
- Original trending/filter work is isolated in `af1af2f`. Preserve unrelated untracked `.cairn/archive/*`; never stage them. Backups: `/private/tmp/harbor-ui-preexisting.patch` and `harbor-ui-preexisting-untracked.json`. Do not load other checkpoints/archive items.
- Committed batches cover shared foundation/gallery, repository/Issue/PR core, Notifications/profile/More, repository/admin/Discussion/code views, Issue relationships, PR lifecycle/reviews, settings/window/context/auth, Discovery, execution/notification targets and Wiki. Exact source/render scope, screenshot names and remaining variants are authoritative in `docs/UI_MIGRATION_CHECKLIST.md`, `docs/UI_VERIFICATION.md` and `docs/UI_COMPONENTS.md`. Source review alone is not visual acceptance. Prior Wiki248 and execution/notification504 capture matrices passed; remaining/new states are explicit in those docs.
- Uncommitted recovery changes add shared compact stale Retry, reaction refresh/empty-readonly feedback, retained conversation controls and11px label, EN/ZH copy and gallery examples. Create/Fork lock inputs/dismissal while pending, retain drafts with inline/resettable failures and preserve selected optional templates on refresh errors. Template/visibility labels are linked. Relationship controls keep an open Fork dialog on refresh error, reuse shared material/tooltips/separators, restore failed Star counts and isolate late responses by repository identity. Six repository regressions reproduce/fix those gaps.
- New `repoActions`, `conversation`, `reactions` and `pins` dev fixtures reconcile explicitly opted-in writes and subsequent reads, copy returned DTOs, enforce relevant permission/identity/count guards and reject unknown business writes. Shared Issue/Wiki fixtures now distinguish same-name forks by owner. Baseline conversation DTOs use the real target argument names. Exact query modes are in UI_COMPONENTS. No real GitHub business data was changed.
- The uncommitted Project batch now guards all five pending dialogs plus archive/delete confirmations, preserves settings/draft/field edits across refreshes, uses current Project permissions for field/item actions and draft reading, and explains empty choice fields. Numeric zero/clear behavior remains. A reproduced cache bug moving the edited row to the end is fixed with in-place replacement. Project descriptions/text values wrap. The `projects` fixture provides all six editable field types, iteration boards, long/empty-option/readonly/closed states and six opted-in mutation families with read reconciliation. Fourteen new UI cases, four fixture cases, preview routing and the strengthened cache regression pass. New visual QA is still pending.
- The uncommitted Gist batch guards pending editor/delete dismissal, names visibility/comment inputs, preserves file/description drafts across refreshed props and keeps comment creation/editing drafts independent. Failures appear only at the affected comment action and reset on reopening delete. Ten UI cases and four fixture cases cover recovery, file lifecycle/old revisions, source/Star/Fork/delete, comment counts and write boundaries; preview routing enforces opt-in. The `gists` modes are documented in UI_COMPONENTS. New visual acceptance remains pending.
- Current checks pass593 tests/124 files, full lint/format/build and separate tsc-b (`/private/tmp/harbor-gist-actions-{check,tsc}.log`). First full run passed592/593 with the existing code-search return test timing out; isolated4/4 and full rerun pass without code/timeout changes (`harbor-gist-actions-check-first.log`, `harbor-gist-actions-code-navigation.log`). Existing harbor-rail exhaustive-deps and bundle-size warnings remain. No new visual captures, commit or push. The previous read-only Git gate was lifted by the user; source files still match the verified 248-file snapshot.
- The remaining work includes advanced Issue lifecycle/comment/candidate states; Gist action visual acceptance and package actions; Project field/draft visual acceptance; nested Discussion replies; archived settings/Pages conditions; release/code transfer feedback; native material/background/accessibility acceptance; formal verified guide values; final checks/CI and actual PR/CodeRabbit loop. Only release.yml currently exists in .github/workflows. Old PR81 proves integration exists, not this branch's review. Final selected screenshot assets must be remotely accessible to reviewers.
- Dependency fix `cd159bf` backports cmdk1.1.1 upstream PR411's async item-id registry in ESM/CJS. Attribution/removal criteria are in `patches/README.md`; tests, browser ARIA linkage and frozen install previously passed. No upstream release/version upgrade is claimed.

## Browser recovery

- Last observed task preview1423 used `pnpm dev:ui --port 1423 --force`; ordinary1420 and earlier1422 were preserved. Check the actual server before reuse. Production previews use intercepted fixtures and ignored `output/playwright/` captures. They do not verify live API correctness or native material.
- uiqa is closed. Existing CLI snapshot/click calls briefly worked in the restricted session, but the before-capture wrapper failed on npm DNS before executing. Cached CLI startup failed writing its default cache; moving only daemon data to allowed temporary storage reached Chrome, which exited SIGABRT. Latest cached CLI `list` reports no browsers. CUA has no browser provider, and native `getApp` rejects both Chrome and Harbor preview as not approved. Do not repeat blind launches or bypass access restrictions.
- Cached CLI entry: `/Users/bytedance/.npm/_npx/31e32ef8478fbf80/node_modules/@playwright/cli/playwright-cli.js`; usual wrapper `/Users/bytedance/.codex/skills/playwright/scripts/playwright_cli.sh -s=uiqa`. No current QA tour is running. The unsuccessful `/private/tmp/harbor-conversation-before.py` had Lock text/timeout corrected but never passed; current modified source cannot establish before evidence with that script.
- Freeze all workspace files and other browser state during QA tours; use fresh goto after HMR. Wait for actual lazy content/skeleton disappearance, not only query idle. Inspect CLI `### Error` blocks even on exit0. Scope menu/dialog controls and scroll within the pane; PR metadata needs pane-bottom positioning. Spinner selector is `svg[aria-label].animate-spin`; pending buttons can change names.
- Preview application invokes go through `src/dev/preview-core.ts`; native window/events remain real. Only browsers use SDK mockIPC. Never assign readonly native bridge internals. Unknown business writes fail. `links=record` records only browser GitHub/gist openers. Primary Repositories opens the full workspace; Discovery detail is code-only.

## Native recovery

- Native acceptance remains incomplete. CUA once returned a real dark Discovery screenshot, but no PNG was saved before the Mac locked again. The attempted `ui-native-initial-dark.png` does not exist. Later CUA inventory confirmed the diagnostic app was running; current access to its window is denied. No desktop backgrounds, both-theme/size or reduced-transparency/motion matrix is claimed. An earlier unlock request remains unanswered; access approval is now an additional observed gate.
- Diagnostic bundle `/private/tmp/Harbor UI Visible Preview.app`, identifier `com.harbor.ui-preview-visible`, config `/private/tmp/harbor-ui-native-preview-visible.json`, devUrl1423. Last observed PID64018 is historical, not proof of the current process. After access returns, inspect the actual window and restart only this task app through CUA to ensure it loads the rebuilt capability. Never shell-drive native GUI or stop unrelated apps.
- `4f3936e` adds only `core:window:allow-set-theme` and `core:window:allow-set-effects` for existing main/about/settings windows: core:default lacked those mutations. Cargo check, two ThemeProvider tests and the signed diagnostic rebuild passed (`harbor-native-appearance-{cargo,tests,build}.log`). Bundle SHA256 b77031fe7e35353ea030f71ae9c471928f299f80d27aac7398130401e8ae10a8. Do not add resize permission; adaptive geometry runs in Rust. Actual appearance behavior remains unverified.
- Native screenshots must use CUA. Previously initialized `fsForNativeScreens` can save CUA screenshot bytes; revalidate bindings/AX indexes before reuse. Controlled backdrop `/private/tmp/harbor-native-backgrounds.html` is ready with Cool/Neutral/Bright and1/2/3 controls; open it through an approved UI channel in a new Chrome window behind Harbor. No OS preference was changed. Record and restore any future appearance/accessibility preferences.

## Next action

Commit and push the verified phase-one changes, create one PR, then process actual CodeRabbit feedback and final checks before merging. Do not start Packages.

## Verification

- `pnpm check`:593 tests/124 files, formatting/lint/build pass; separate `pnpm exec tsc -b` passes. Logs `/private/tmp/harbor-gist-actions-check.log` and `harbor-gist-actions-tsc.log`. Existing lint/chunk warnings remain.
- Prior browser evidence covers both themes/languages at900×620 and1440×900, plus Settings600×500 and About500×400; exact scope is in UI_VERIFICATION. New recovery and action fixtures have no browser/native acceptance yet.
- Prior Rust cargo check and signed native preview build pass. Remaining UI variants, native visuals, final CI/current-task PR and CodeRabbit review remain unverified.

Success: phase-one local code checks pass and handoff/PR materials are prepared. PR publication, actual CodeRabbit review and merge are now authorized and executable, but remain incomplete. Full-site/native work is deferred by the latest user instruction.
