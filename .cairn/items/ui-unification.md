# Harbor UI unification

## Goal

Execute `/private/tmp/harbor-ui-refactor-handoff.md`: migrate all owned UI, verify browser and native appearance, deliver an unmerged PR, and fix/recheck actual CodeRabbit feedback on the final commit.

## Current state

- Workspace `/Users/bytedance/Documents/Work/Code/harbor`; branch `refactor/unified-harbor-ui`, base `03709c2`, origin `Excelius-Wang/harbor`. No push or current PR. User authorized commits, push, PR and review replies; no merge/release. Push/PR only after full local implementation and acceptance.
- Original trending/filter work is isolated in `af1af2f`. Preserve unrelated untracked `.cairn/archive/*`; never stage them. Backups: `/private/tmp/harbor-ui-preexisting.patch` and `/private/tmp/harbor-ui-preexisting-untracked.json`.
- Completed local batches through `83e42fa`: shared foundation/gallery, repository/Issue/PR core, Notifications/profile/More, repository tabs, administration/Discussions, code/history/search, Issue relationships, PR lifecycle/reviews, settings/window/theme/shortcuts. The verified context/auth batch is committed as `b9558d3`. Exact source/render coverage, screenshot patterns, limits and checks are authoritative in `docs/UI_MIGRATION_CHECKLIST.md`, `docs/UI_VERIFICATION.md` and `docs/UI_COMPONENTS.md`. Earlier core coverage does not imply advanced variants passed.
- Context/auth batch is verified: shared command/rail material, named command input and accurate hints, one scrollable Agent response with fixed input, late-context response cancellation, retryable auth availability, shared avatar, follow-error toast and Toaster under ThemeProvider. Five new regressions; final full check passes 512 tests/114 files and separate tsc-b. All tours complete: 176 current captures. Scripts/logs `/private/tmp/harbor-context-{qa,states}` and `harbor-command-final-qa`; final code logs `harbor-context-final-{check,tsc}.log`. Representative captures visually inspected. No application business writes reached GitHub.
- Shared/gallery batch is verified and included in the local commit titled `refactor: complete shared component gallery and states`. Localized ModeToggle/Breadcrumb/Pagination/Sonner defaults, 16 px FieldGroup/FieldSet spacing, transparent separators, correct mixed-checkbox indicator, and custom-max Progress fill. The existing Progress test is preserved with one new regression. Final full check513 tests/114 files and tsc pass (`harbor-gallery-verified-{check,tsc}.log`). All tours complete:192 gallery,72 browser preference/background and12 existing-form captures, refreshed after anchoring ModeToggle's moon icon inside its trigger. Scripts/logs and exact coverage are in UI_VERIFICATION.md. Browser media emulation passes opaque/no-filter and reduced-animation assertions; native remains separate.
- Discovery batch is verified and included in the local commit titled `refactor: unify discovery lists and recovery states`. All5 search kinds and feed have controlled read fixtures; query/filter/cache semantics remain. Shared alignment/rows/metadata/reading/loading and stale feedback, separate feed continuation error, and parent-owned scroll restoration are implemented. Four new regressions plus the augmented developer-return case pass. Full check517 tests/114 files and tsc pass (`harbor-discovery-final-{check,tsc}.log`). All tours complete:36 before,72 core,96 filter/return and176 state captures; corrected8 next-error footers show enabled Load more. Ten browser-recorded GitHub/gist opener destinations match expected URLs (`harbor-discovery-links-complete.log`). Exact scripts, evidence and boundaries are in UI_VERIFICATION.md. Native observation remains open.
- Execution/notification batch is verified: metadata/list stale notices and Retry, pane-local initial states,24px suite title,13px execution/check text, narrow Actions controls, inset Alerts and full-width Discussion titles. Count-neutral English summaries retain the existing Chinese copy. Controlled workflow action/read reconciliation and9 notification targets are wired; downloads return DTOs without creating files. Four execution/checks regressions and two fixture cases pass. Final full check523 tests/116 files and separate tsc pass (`harbor-actions-verified-{check,tsc}.log`).504 current captures pass:112 core,160 read states,128 mutations,80 notification routes/returns,24 shared consumers.24 affected English captures refreshed after the copy correction. Exact evidence, transient harness failures and fixture boundaries are now in UI_VERIFICATION/UI_COMPONENTS/checklist. No QA tour is running. Local commit is authorized; do not stage unrelated archive files.
- Browser uiqa was reopened headless at1423 after its daemon closed, pid40246. The complete mutation/notification/consumer rerun passes (`harbor-actions-remaining.log`, individual `*-verified.log` outputs); default headed browser is untouched. Core and dark states use `*-verified.log`; light states passed before an isolated navigation failure in `harbor-actions-states-final.log`. No production fix is attributed to that transient reset. Scripts wait for real jobs/lazy Markdown; rows are scoped to article, and Commit has two existing Back controls. Freeze all workspace files/browser state during future tours; use fresh goto after HMR. The current Wiki fixture supersedes the earlier temporary draft.
- Wiki batch is verified: real empty history, retained history/revision/comparison with stale Retry, initial revision/comparison Retry, raw/no-hunk patch fallback, incomplete-comparison feedback, wrapping/selected revision rows, reading material, long-title Close spacing and inline/resettable restore errors. Existing current/deleted/offline/archived/readonly restrictions remain. Shared notice message12rem basis stacks Retry in narrow rails and stays inline in wide panes.530 tests/117 files and tsc pass (`harbor-wiki-final-{check,tsc}.log`); seven new UI/fixture cases.34 before and248 current captures complete (128 core,112 states,8 shared PR notice). No browser tour is running. Final scripts/logs and exact boundaries are in UI_VERIFICATION/UI_COMPONENTS/checklist; local commit is authorized.
- Next bounded source audit is recorded in `/private/tmp/harbor-next-ui-audit.md`. ReactionBar and ConversationControls hide retained-query errors; repository relationship errors hide cached controls/open Fork dialog. Fork pending inputs/close behavior and toast-only failure, hardcoded star amber, compact labels/tooltips, conversation10px label and remaining controlled action/candidate states need the next batch. Readme, execution icons, reaction-provider and comment-adapter source audits are recorded in the checklist; preserve their sanitizer/routing/cache/rollback semantics. Still unread there: issue-pin-action, issue-shared and repository-create-dialog. Remaining More fields/drafts/Gists/package restore, nested replies, archived settings/Pages, release/code actions stay in the project inventory.
- Separate dependency fix `cd159bf` backports cmdk 1.1.1 upstream PR411's async item-id registry, ESM/CJS. Tests and browser Teams input-to-option ARIA linkage pass; attribution/removal criteria in `patches/README.md`. Frozen lockfile install passed; no version upgrade or claim of upstream release.
- Remaining work: advanced Wiki/code/Issue/repository/More action variants listed in the checklist; formal guide/AGENTS entry points; native background/transparency/motion acceptance; final checks/CI, PR and actual CodeRabbit loop. Old PR81 proves integration exists but is not this branch's review. Final selected screenshot assets need remote publication with the PR.

## Preview and recovery

- Task preview1423 runs `pnpm dev:ui --port 1423 --force`, exec58868. Original1420 and task1422 remain untouched. The1423 optimizer contains patched cmdk `getItemIdByValue`.
- Browser wrapper `/Users/bytedance/.codex/skills/playwright/scripts/playwright_cli.sh -s=uiqa` is reliable headless. Production React captures use intercepted fixtures, stored in ignored `output/playwright/`. These are local evidence, not remote reviewer links or live API validation.
- Never mutate application source or another browser state during a QA tour. Fresh goto after HMR. Source/read-only docs/scripts are safe. Wait for actual Markdown/options rather than only query idle. Spinner selector is `svg[aria-label].animate-spin`; pending button names may include Loading. Sonner uses `data-sonner-theme`. A CLI exit0 alone is insufficient: inspect `### Error` blocks in tour logs.
- Scoped states: `?state=loading|empty|error|stale&commands=comma,separated,IPC,names`; specific pr/update/shortcut/auth/repo/agent parameters are in UI_COMPONENTS.md. Unknown business writes fail. Application SDK imports are rewritten to `src/dev/preview-core.ts` in ui-preview only; never assign readonly native bridge internals. Browser alone uses SDK mockIPC; native window/events remain real. Browser plugin mocks do not establish native behavior.
- Primary Repositories opens the full tab workspace; Discovery detail is code-only. English repo tab is Pull requests, primary nav Pull Requests, creation selector Compare branch. Use pane-local scrolling; sticky PR metadata requires scrolling to pane bottom before positioning lower controls. Command logs contain embedded full dictionaries: inspect only headings/errors, never broad rg Error.

## Native / external gates

- Native material remains unverified. Original native process was preserved. Diagnostic `/private/tmp/Harbor UI Visible Preview.app` uses `/private/tmp/harbor-ui-native-preview-visible.json` (visible=true, com.harbor.ui-preview-visible, devUrl1423); last PID15241. CUA inventories list it, but getApp returns -10005/cgWindowNotFound before providing a binding. WebKit logs previously reported visible1/hidden0/occluded1. One cold launch after the bridge fix did not establish visibility. Do not repeat blind launches or bypass CUA with native automation.
- Read-only ioreg evidence, rechecked2026-09-06 03:29 UTC: the console session has `CGSSessionScreenIsLocked=true`. An async user request now asks to unlock the Mac and report completion. This identifies current lock state, not the cause of all earlier failures. Await external state before another native observation. CUA was reset and docs read; no target binding exists after failed getApp. Native screenshots, cool/neutral/bright backgrounds and reduced-transparency fallback remain required.

## Next action

Implement and verify remaining conversation, reaction and repository relationship/create controls.

## Verification

- Current `pnpm check`:530 tests/117 files plus complete-source lint/format/build; separate tsc passes. Logs `/private/tmp/harbor-wiki-final-check.log`, `/private/tmp/harbor-wiki-final-tsc.log`. Wiki248-image matrix and prior execution/notification504-image matrix are complete.
- Browser matrices cover both themes/languages at900×620 and1440×900, plus Settings600×500 and About500×400. Exact page/state/focus/persistence evidence is in UI_VERIFICATION.md.
- Baseline Rust cargo check and native preview build pass. Native visuals, remaining migration, final CI/current-branch PR/CodeRabbit remain unverified.

Success: current code checks and completed browser batches pass; the overall Goal remains active and incomplete.
