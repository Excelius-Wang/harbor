async page => {
 await page.goto('http://localhost:1423/');
 await page.emulateMedia({reducedMotion:'no-preference'});await page.setViewportSize({width:900,height:900});
 await page.evaluate(()=>{localStorage.setItem('i18nextLng','zh');localStorage.setItem('tauri-ui-theme','dark')});
 await page.goto('http://localhost:1423/?calendar=real&profile=long');
 await page.getByRole('button',{name:'账户',exact:true}).click();
 await page.locator('.harbor-contribution-calendar').scrollIntoViewIfNeeded();
 await page.getByRole('button',{name:'月份',exact:true}).click();
 const phase=async value=>page.waitForFunction(v=>document.querySelector('.harbor-calendar-companion')?.dataset.phase===v,value);
 await page.locator('[data-contribution-day="2026-09-08"]').click();await page.mouse.move(5,5);await phase('encounter');
 if(await page.locator('.harbor-calendar-companion').getAttribute('data-encounter')!=='bug')throw Error('bug');
 await page.screenshot({path:'output/playwright/calendar-map/bug.png'});
 await page.locator('[data-contribution-day="2026-09-02"]').click();await page.mouse.move(5,5);await phase('reward');
 await page.waitForTimeout(220);await page.screenshot({path:'output/playwright/calendar-map/chest.png'});
 const lid=await page.locator('.harbor-companion-chest').evaluate(e=>getComputedStyle(e,'::after').transform);
 if(lid==='none')throw Error('chest not opening');
 await page.getByRole('button',{name:'全年',exact:true}).click();
 const first=page.locator('[data-contribution-day]').first(),last=page.locator('[data-contribution-day]').last();
 await first.click();await page.mouse.move(5,5);await phase('idle');
 const scroll=()=>page.locator('.harbor-contribution-period [data-radix-scroll-area-viewport]').evaluate(e=>e.scrollLeft);
 const left=await scroll();await last.click();await page.mouse.move(5,5);await phase('idle');const right=await scroll();
 if(right<=left)throw Error('scroll coverage');
 await first.click();await page.waitForTimeout(100);
 await page.evaluate(()=>{Object.defineProperty(document,'hidden',{configurable:true,get:()=>true});document.dispatchEvent(new Event('visibilitychange'))});
 await page.waitForFunction(()=>document.querySelector('.harbor-calendar-companion').dataset.active==='false' && document.querySelector('.harbor-companion-position').getAnimations().every(a=>a.playState==='paused'&&!a.pending));
 const point=()=>page.locator('.harbor-companion-position').evaluate(e=>({x:getComputedStyle(e).left,y:getComputedStyle(e).top}));
 const stopped=await point();await page.waitForTimeout(800);const after=await point();
 if(JSON.stringify(stopped)!==JSON.stringify(after))throw Error('in-flight pause');
 await page.evaluate(()=>{delete document.hidden;document.dispatchEvent(new Event('visibilitychange'))});await phase('idle');
 await first.click();await last.click();await phase('idle');
 if(await last.getAttribute('aria-pressed')!=='true'||await page.locator('[data-contribution-day][aria-pressed="true"]').count()!==1)throw Error('replacement');
 await page.setViewportSize({width:1440,height:900});
 await page.waitForTimeout(700);
 const aligned=await page.locator('.harbor-companion-position').evaluate(e=>{const grid=e.closest('.harbor-contribution-map'),d=grid.querySelector('[data-contribution-day][aria-pressed="true"]'),r=e.getBoundingClientRect(),g=grid.getBoundingClientRect(),c=d.getBoundingClientRect();return Math.abs(r.x+r.width/2-Math.max(g.x+r.width/2,Math.min(g.right-r.width/2,c.x+c.width/2)))<1});
 if(!aligned)throw Error('resize');
 await page.locator('.harbor-markdown h2').last().scrollIntoViewIfNeeded();
 await page.waitForFunction(()=>document.querySelector('.harbor-calendar-companion').dataset.active==='false');
 await page.locator('.harbor-contribution-map').scrollIntoViewIfNeeded();
 await page.waitForFunction(()=>document.querySelector('.harbor-calendar-companion').dataset.active==='true');
 await page.getByRole('heading',{name:'个人主页',exact:true}).click();
 if(await page.locator('[data-contribution-day][aria-pressed="true"]').count())throw Error('outside');
 return {bug:true,lid,scroll:{left,right},inFlight:{stopped,after},replacement:true,resize:true,offscreen:true,outside:true};
}
