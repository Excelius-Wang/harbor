# PR96 review follow-up

Initial reviewed head: 5db70458b2a848c17338c731a2f3c7aa1b233ddf.

Copilot review 5179537633 posted two findings. Both are fixed:
- Normalize real PullRequestEvent closed+merged payloads to `merged`; a Rust regression covers merged, unmerged, missing flags and other event types.
- Use sentence-case English actions after repository names without changing shared sentence-initial translations; an English regression checks both contexts.

Confirmed supplementary notes are also fixed: root README404/409 returns absence, reduced motion preserves a static encounter, selected keyboard focus retains its blue ring, and a 350ms entrance window prevents later refreshed cells from replaying arrival. Three-parameter color/status semantics and ordinary error handling remain unchanged.

CodeRabbit review 5179512598 posted five findings, all addressed: current PR recovery state; localhost navigation before storage in verification scripts; explicit Chinese initialization in Chinese-only scripts; and normalized branch reference in the activity preview fixture. Equivalent cold-storage bootstrap sites were fixed together.

Validation after fixes:
- `VITEST_MAX_WORKERS=1 pnpm check`: 689 tests in 140 files, format/lint/type/build pass. `/tmp/pr96-final-check.log`.
- `cargo check` and 8 focused native Profile tests pass. `/tmp/pr96-cargo-final.log`, `/tmp/pr96-native-final.log`.
- 21 focused frontend tests pass. `/tmp/pr96-focused.log`.
- Eight-case browser review passes from about:blank, including static reduced-motion chest visibility and distinct selected keyboard focus. `/tmp/pr96-copilot-browser.log`.
- Chinese README states pass after seeding English and returning to about:blank. `/tmp/pr96-cold-chinese-states.log`.
- Verification scripts parse and `git diff --check` passes.

Existing hook and bundle warnings remain nonblocking. Original CodeQL checks passed. This record describes local fixes; Copilot re-review and updated remote checks are the remaining merge gates.
